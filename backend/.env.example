# Server Configuration
PORT=8000

# Database Configuration
DB_HOST=localhost
DB_PORT=3306
DB_USER=app_user
DB_PASSWORD=secure_password
DB_NAME=interviewbuddy

# For Render.com deployment, PORT will be set automatically
# DB_* variables will be set by Render's database service
